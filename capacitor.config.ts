import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.aufzuneuenwelten.learnlanguageapp',
  appName: 'LearnLanguageApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
