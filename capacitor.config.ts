import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.learnLanguageapp',
  appName: 'Learn Language App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
